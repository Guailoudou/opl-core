# OPL Core Web 前端交互与 UX 审查

审查日期：2026-09-05  
审查范围：`web/index.html`、`web/style.css`、`web/app.js`、`web/manifest.json`，并追踪 WebSocket 初始化与内容加载流程。  
方法：静态代码审查、交互状态流追踪、响应式规则检查，以及 `ui-ux-pro-max` 的 Accessibility、Touch & Interaction、Layout、Forms & Feedback、Navigation 检查表和针对性本地规则检索。  
限制：本报告未连接真实 Core、屏幕阅读器或浏览器自动化环境；颜色、焦点和 320/375/560/900px 布局仍需在实现后做运行时复测。

## 结论

当前界面已经具备原生语义控件、哈希深链、页面切换后的焦点管理、原生 `dialog`、危险操作确认、移动侧栏焦点隔离、44px 主按钮、响应式断点和 `prefers-reduced-motion` 等良好基础。主要问题不是视觉重做，而是连接/保存状态没有统一封锁、可自定义颜色缺少可访问性约束，以及动态状态和失败恢复对辅助技术与普通用户都不够明确。

建议先处理 4 项高严重度问题，再处理 6 项中严重度问题。低严重度项目可随相邻修改一并完成。

## 高严重度

### H1. Core 尚未就绪或断线后，业务控件仍显示为可操作

- 位置：`web/index.html:31-72`；`web/app.js:30-55`、`web/app.js:96-102`、`web/app.js:452-503`
- 证据：初始 HTML 中“新建连接”“启动”“创建网络”“加入网络”等按钮默认启用；只有首次 `core.getState` 成功并执行 `render()` 后才按状态禁用。断线时 `onclose` 只更新 Core 状态、拒绝 pending 请求并重连，没有重新禁用业务控件。此时 `toggleP2P`、`hostToggle` 等处理器会读取可能为 `null` 或已过期的 `state`。
- 影响：首屏快速点击可能抛出脚本异常；断线后界面仍承诺可执行操作，用户只能在点击后收到“Core 未连接”，且可能连续产生错误。
- 建议：默认将依赖 Core 的控件置为 `disabled`；增加单一 `ready/connected` UI 门控，在 `onopen` 完成初始化后解锁，在 `onclose` 立即锁定并保留只读导航、日志和外观设置。断线状态应有可见文本“正在重连…”，而不只更新状态点。
- 验收：以慢速启动加载页面，初始化完成前所有 Core 操作不可点击；运行中断开 WebSocket 后 100ms 内相关控件禁用；重连成功后按最新状态恢复；控制台无 `state is null` 异常。

### H2. 隧道保存链路缺少忙碌态，允许并发提交和乱序回写

- 位置：`web/app.js:143-164`、`web/app.js:244-267`、`web/app.js:288-310`、`web/app.js:460-483`
- 证据：`saveApps()` 在请求期间未设置全局保存状态，也未禁用卡片内启用、删除、排序等操作；添加/编辑表单在请求前关闭对话框且不等待保存；快速添加的外层 `run()` 在打开对话框后立即结束，确认按钮触发 `saveApps()` 时没有 `disabled` 或 `aria-busy`。
- 影响：连续切换、删除、排序或重复确认会产生多个 `tunnel.replace`；响应乱序时旧配置可能覆盖新操作。失败发生在对话框关闭后，用户还要重新打开并重填。
- 建议：复用一个最小的 `savingApps` 状态，使所有隧道编辑入口在 `tunnel.replace` 未完成时禁用并显示“保存中…”；添加/编辑失败时保留或重新打开原对话框及输入；确认处理器必须 `await saveApps()`。避免新增队列或状态机，单飞请求已足够。
- 验收：把 `tunnel.replace` 延迟 2 秒，期间无法再次切换/删除/排序/提交；成功只出现一次确认；失败恢复旧列表并保留用户输入和可重试路径。

### H3. 强调色允许任意值，会直接破坏 CTA、品牌标记和状态的对比度

- 位置：`web/index.html:72`；`web/style.css:1-3`；`web/app.js:539-544`
- 证据：颜色选择器接受任意 RGB，并直接写入 `--accent`；`.primary` 和 `.brand-mark` 始终使用白字。选择白色或浅黄色时文字/背景接近 1:1，远低于普通文字 4.5:1。固定的 `--accent-hover` 也不会随自定义强调色变化，交互前后可能完全换色。
- 影响：主要操作可能不可读；浅色/深色主题都可能失去品牌和状态层级。这是用户可稳定触发的 WCAG 对比度问题。
- 建议：最小方案是只提供一组经浅色/深色主题验证的预设强调色；若必须保留任意取色，则保存前计算与白/深色前景的对比度，自动选前景色并派生 hover/focus 色，不合格时给出明确提示。分别验证两套主题，不能只测默认蓝色。
- 验收：预设或任意可保存颜色下，主按钮文字达到 4.5:1，焦点指示和控件边界达到 3:1；hover/pressed 仍与基色同色系；浅色、深色各运行一次自动对比度检查。

### H4. 长时间“加入网络”没有超时、取消或恢复入口

- 位置：`web/app.js:169-180`、`web/app.js:497-503`
- 证据：`room.join` 显式传入 `timeout = 0`；`joinBusy` 期间按钮被禁用，因此用户看见“连接中…”却不能取消。若请求未返回，界面会无限停留，只有刷新页面可以恢复。
- 影响：弱网、服务端无响应或协议异常时形成死胡同；用户无法判断仍在重试还是界面已卡死。
- 建议：保留 Core 的后台重试能力，但 UI 必须提供“取消连接/停止重试”；也可在合理等待后将禁用按钮变为可点击的取消按钮，并显示当前阶段与最后错误。不要用无限旋转器替代恢复路径。
- 验收：模拟 `room.join` 永不返回，用户无需刷新即可取消；取消后字段恢复可编辑；失败信息包含原因和“重试”动作；键盘和屏幕阅读器均可操作。

## 中严重度

### M1. 预设隧道仍被无关的端口必填校验阻止

- 位置：`web/index.html:78`；`web/app.js:288-307`、`web/app.js:546`
- 证据：远程端口、本地端口始终带 `required`，浏览器原生校验先于 `onsubmit`；但选择预设后，代码实际从 `selectedPreset.tunnel` 取端口，两个手填字段不会被使用。
- 影响：用户选择预设后仍必须填写无效输入，流程与“使用预设”的含义冲突。
- 建议：选择预设时隐藏并禁用手动端口/协议字段，取消预设时恢复；若产品期望预设只是参考，则反向调整代码和说明，避免两套模型并存。
- 验收：选择有效预设，仅填写 UID 即可提交；切回“不使用预设”后端口字段恢复、必填且聚焦首个错误。

### M2. 表单错误主要显示在全局顶部，缺少字段关联和就地恢复

- 位置：`web/index.html:34`、`web/index.html:55-57`、`web/index.html:72`、`web/index.html:78-79`；`web/app.js:288-310`、`web/app.js:348-363`、`web/app.js:446-450`、`web/app.js:497-503`
- 证据：`showError()` 将多数错误发送到页面顶部 `#alert` 或对话框顶部，字段没有稳定的错误节点与 `aria-describedby`；邀请码服务端错误不会回到 `joinInvite` 附近。名称字段设置了 `required`，但标签没有 `*`，与“带 * 的项目为必填项”矛盾。
- 影响：长页面上用户难以定位问题；屏幕阅读器虽然听到 alert，却不知道哪个字段需要修复。
- 建议：对 UID、端口、邀请码、共享带宽和 IP 列表保留简短的字段级错误，使用 `aria-describedby`/`aria-invalid`；全局错误只承载连接或系统级失败。修正必填标记一致性。
- 验收：每个无效字段旁显示“原因 + 修复方法”；错误和字段建立程序化关联；提交失败后焦点落在首个无效字段（多错误时可先聚焦摘要）。

### M3. 共享带宽的 HTML 与 JavaScript 校验不一致

- 位置：`web/index.html:34`；`web/app.js:446-450`
- 证据：HTML 声明 `max="100000"`，脚本只检查整数和非负数，未检查上限，也没有调用 `checkValidity()`；`change` 处理器可以继续提交超出 HTML 上限的值。
- 影响：浏览器显示字段无效时仍可能保存，用户无法信任输入约束。
- 建议：复用原生约束：保存前调用 `reportValidity()`，或让脚本与 `min/max` 同源检查；错误放在字段下方。
- 验收：输入 `100001`、负数、小数、空值均不会请求 `config.replace`，且给出一致错误；`0` 和 `100000` 正常保存。

### M4. 动态状态对屏幕阅读器的播报过多与不足并存

- 位置：`web/index.html:22`、`web/index.html:27`、`web/index.html:37`、`web/index.html:51-64`；`web/app.js:90-93`、`web/app.js:125-210`
- 证据：移动页头只显示无文本、无可访问名称的状态点；成员数、网络状态和错误变化没有统一的上下文播报。反之，整个 `#tunnelList` 带 `aria-live="polite"`，每次状态刷新都会 `replaceChildren`，可能反复朗读完整列表。
- 影响：辅助技术用户在移动端不知道 Core 状态；频繁状态事件又可能造成整列表噪声。
- 建议：给页头增加视觉隐藏的完整状态文本；把网络状态/错误放入一个 `role="status"` 且 `aria-atomic="true"` 的简短区域；移除列表容器 live 属性，只在保存成功或单条状态实质变化时播报完整短句。
- 验收：状态变化只朗读一次“Core 已断开，正在重连”等完整语句；列表刷新不重复朗读所有隧道；状态仍有文字而非只靠颜色表达。

### M5. 内容加载的空状态和失败状态缺少恢复动作

- 位置：`web/index.html:70-74`；`web/app.js:556-571`
- 证据：公告数组为空时 `replaceChildren()` 生成空白页面；获取失败只显示文本，没有就地重试按钮。节点列表失败时只有一个失败选项，设置页没有单独重试；“刷新公告”实际还会静默重载更新日志、鸣谢、预设和节点，动作范围与标签不一致。
- 影响：用户无法区分“没有公告”和“渲染失败”，节点失败后不知道如何恢复；刷新行为不可预测。
- 建议：公告提供“暂无公告”空状态；失败状态包含“重试”按钮。将内容加载按资源拆为最小独立函数，让“刷新公告”只刷新公告，节点失败可在设置页重试。
- 验收：分别模拟空数组、HTTP 500、离线和恢复联网；每种状态都有明确文案，失败可在原位置重试且不会刷新无关区域。

### M6. 启用复选框的可点击区域小于 Web 目标尺寸建议

- 位置：`web/style.css:6`；`web/app.js:152`
- 证据：`.enable-toggle` 为 18×18px，外层 label 没有最小高度/内边距；虽然文字也能点击，但整体高度仍约为一行文字，低于 WCAG 2.2 Web 目标 24×24 CSS px 的常规门槛，也低于项目其余 44px 控件基线。
- 影响：触控、运动障碍和高缩放用户更容易误触，且与同一卡片按钮的尺寸不一致。
- 建议：让 label 本身至少 44px 高并带水平点击区域；保持原生 checkbox 语义，无需自定义开关组件。
- 验收：浏览器测量启用控件的可点击 label 至少 44px 高；文本、方框均可触发；键盘 Space 操作和焦点环正常。

## 低严重度

### L1. 桌面基础字号偏小，日志和辅助文字更小

- 位置：`web/style.css:1-2`、`web/style.css:11`
- 证据：桌面根字号为 14px，部分说明和表头为 12px，只有小于 560px 才升到 16px。
- 影响：桌面高 DPI、远距离或低视力场景的可读性偏弱。
- 建议：默认正文使用 16px；确需紧凑的数据/日志区域可保留 13–14px，并验证 200% 缩放无截断和横向页面滚动。

### L2. 复制失败可能仍提示成功，空值点击没有反馈

- 位置：`web/app.js:375-379`；相关入口见 `web/app.js:445`、`web/app.js:494-506`、`web/app.js:511`
- 证据：旧式 `document.execCommand('copy')` 的布尔返回值被忽略，随后总是显示成功 toast；值为空时直接返回，无提示且按钮仍可能启用。
- 影响：用户以为邀请码/IP/日志已经进入剪贴板，粘贴时才发现失败。
- 建议：仅在 Clipboard API 或 fallback 明确成功后提示；失败时给出“复制失败，请手动选择”的恢复路径；空值时禁用复制按钮。

### L3. Toast 显示时间短于技能规则建议

- 位置：`web/app.js:364`
- 证据：toast 固定 2.6 秒后隐藏；技能规则建议一般自动消失通知保持 3–5 秒。
- 影响：较长中文提示、放大显示或注意力切换时可能来不及阅读。
- 建议：改为至少 3 秒；错误不要使用会自动消失的 toast，继续保留持久 alert。

### L4. 新窗口链接没有告知用户上下文会变化

- 位置：`web/index.html:74`
- 证据：帮助、作者主页、GitHub 均使用 `target="_blank"`，可见标签和可访问名称未说明“在新窗口打开”。
- 影响：屏幕阅读器和认知障碍用户可能对焦点/窗口变化感到困惑。
- 建议：在可见辅助文字或视觉隐藏文本中补充“新窗口打开”；如果没有保留当前控制台的强需求，直接同窗打开更简单。

## 已确认的良好实践（修复时应保留）

- `web/index.html:5` 使用正确 viewport，未禁用缩放。
- 导航使用原生按钮并维护 `aria-current`；哈希支持关键页面深链，页面切换后焦点移动到当前 `h1`（`web/app.js:413-433`）。
- 移动侧栏使用 `aria-expanded`、`aria-controls`、`inert`、Escape 和遮罩关闭，并能合理归还焦点（`web/app.js:391-438`）。
- 对话框使用原生 `<dialog>`，有明确关闭/取消入口；危险操作普遍有确认。
- 主要按钮和输入高 44px，布局在 900px/560px 断点重排；宽表被限制在带说明的可横向滚动容器中。
- CSS 尊重 `prefers-reduced-motion`；浅色/深色主题使用语义变量，默认蓝色主按钮本身具备较好对比度。
- 拖拽排序不是唯一方式，已有“上移/下移”键盘与单指替代，符合技能库的 Dragging Movements 要求。
- 错误区域使用 `role="alert"` 并主动聚焦；普通成功通知使用 `role="status"`，方向正确。

## 推荐修复顺序

1. 用一个连接就绪门控解决 H1，并顺带完善移动状态播报（M4）。
2. 用一个 `savingApps` 单飞状态解决 H2，再修正预设字段启停（M1）。
3. 限制/校验强调色并补做两主题对比度检查（H3）。
4. 给加入流程增加取消/超时恢复（H4）。
5. 合并处理字段级错误与原生约束（M2、M3）。
6. 补齐内容空/错/重试状态和小尺寸控件（M5、M6），最后随手处理低严重度项目。

## 修复后最小验证矩阵

| 场景 | 检查 |
|---|---|
| 初始加载、Core 断开、自动重连 | 控件门控、状态文字、无脚本异常、焦点不被抢走 |
| 隧道保存延迟/失败/连续点击 | 单飞、忙碌播报、回滚、输入保留、可重试 |
| 加入请求无响应/失败/取消 | 无死胡同、字段恢复、错误含恢复动作 |
| 预设/手动隧道两种模式 | 只校验当前可见且生效的字段 |
| 浅色/深色 + 最浅/最深允许强调色 | 文本 4.5:1，控件与焦点 3:1，状态不只靠颜色 |
| 320px、375px、560px、900px、桌面；横屏 | 无页面级横向滚动，操作不遮挡，表格容器可滚动 |
| 键盘 Tab/Shift+Tab/Enter/Space/Escape | 所有动作可达、焦点可见、模态框/侧栏可退出 |
| NVDA/VoiceOver 基本烟测 | 页面切换、断线、保存、失败只播报必要的完整短句 |
| 200% 缩放与 reduced-motion | 无文字截断/控件覆盖；无非必要动画 |

## 规则检索记录

- `keyboard focus modal --domain ux`：命中 Focus States、Focus Not Obscured、Keyboard Navigation。
- `error recovery retry --domain ux`：命中 Error Recovery、Error Placement、Focusable Error Summary。
- `dragging movements alternative --domain ux`：确认现有上移/下移按钮满足拖拽替代要求。
- `loading button async feedback --domain ux`：命中 Loading Buttons、Loading Indicators。
- `responsive table overflow --stack html-tailwind`：用于核对原生 Web 的溢出和响应式布局原则；项目并未使用 Tailwind，未采纳其框架特定写法。
